spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jankalyan_dev?createDatabaseIfNotExist=true
    username: root
    password: password # Replace with secure environment variable in actual environment
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080
